# Benchmarks

Benchmark definitions and scoring code belong here. Private benchmark inputs,
expected values, rendered pages, extracted text, and run outputs stay outside
the repository and are selected through a local configuration file.

See [the benchmark plan](../docs/benchmarking.md).
