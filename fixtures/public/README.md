# Public Fixtures

Only synthetic or intentionally redistributable fixtures may be added here.
Each fixture must document its source, license or generation method, and the
specific behavior it tests.

Private user documents must never be added, including with `git add -f`.
